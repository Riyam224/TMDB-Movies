import 'package:flutter/material.dart';

class AppColors {
  // 🌸 Soft pastel palette (from your image)
  static const cream = Color(0xFFFCF9EA); // base background
  static const mint = Color.fromARGB(255, 54, 203, 188); // minty blue
  static const blush = Color(0xFFFFA4A4); // main pink
  static const palePink = Color(0xFFFFBDBD); // soft pink accent

  // 🌙 You can keep these for dark/light theme compatibility
  static const navyBlue = Color(0xFF11224E);
  static const orange = Color(0xFFF4813F);
  static const white = Color(0xFFFFFFFF);
}
